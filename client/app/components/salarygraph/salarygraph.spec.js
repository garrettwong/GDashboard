import SalarygraphModule from './salarygraph'
import SalarygraphController from './salarygraph.controller';
import SalarygraphComponent from './salarygraph.component';
import SalarygraphTemplate from './salarygraph.html';

describe('Salarygraph', () => {
  /*
  let $rootScope, makeController;

  beforeEach(window.module(SalarygraphModule));
  beforeEach(inject((_$rootScope_) => {
    $rootScope = _$rootScope_;
    makeController = () => {
      return new SalarygraphController();
    };
  }));

  describe('Module', () => {
    // top-level specs: i.e., routes, injection, naming
  });

  describe('Controller', () => {
    // controller specs
    it('has a name property [REMOVE]', () => { // erase if removing this.name from the controller
      let controller = makeController();
      // expect(controller).to.have.property('name');
    });
  });

  describe('Template', () => {
    // template specs
    // tip: use regex to ensure correct bindings are used e.g., {{  }}
    it('has name in template [REMOVE]', () => {
      // expect(SalarygraphTemplate).to.match(/{{\s?\$ctrl\.name\s?}}/g);
    });
  });

  describe('Component', () => {
      // component/directive specs
      let component = SalarygraphComponent;

      it('includes the intended template',() => {
        expect(component.template).to.equal(SalarygraphTemplate);
      });

      it('invokes the right controller', () => {
        expect(component.controller).to.equal(SalarygraphController);
      });
  });
  */
});
