import template from './treemap.html';
import controller from './treemap.controller';
import './treemap.styl';

let treemapComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default treemapComponent;
