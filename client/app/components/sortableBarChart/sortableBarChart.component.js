import template from './sortableBarChart.html';
import controller from './sortableBarChart.controller';
import './sortableBarChart.styl';

let sortableBarChartComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default sortableBarChartComponent;
